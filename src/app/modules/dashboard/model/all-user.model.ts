export class AllUserModel {
    users: [
        {
            email?: string,
            firstName?: string,
            isActive?: boolean,
            lastName?: string,
            fullName?: string,
            role?: string,
            userName?: string,
            _id?: string
        }
    ];
    success: boolean
}
